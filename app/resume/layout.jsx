export default function ResumeLayout({ children }) {
    return (
      <html lang="en">
        <head>
          <title>HOTSHIT</title>
        </head>
        <body>{children}</body>
      </html>
    );
  }